{
  "version": 1,
  "builds": [
    {
      "src": "/server/api/index.js",
      "use": "@vercel/node"
    },
    {
      "src": "/client/dist/*",
      "use": "@now/static"
    }
  ],
  "routes": [{ "src": "/api/server/(.*)", "dest": "/api/server/$1" }]
}
